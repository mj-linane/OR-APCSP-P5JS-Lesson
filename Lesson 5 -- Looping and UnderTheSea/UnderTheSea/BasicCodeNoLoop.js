// Setup the canvas
function setup() {
  createCanvas(windowWidth, windowHeight);
  background('DarkBlue');
}

function draw() {
  drawFish();
  drawFish();
  drawStarfish();
  drawSeagrass();
}


// Draw a five pointed star with a high strokeWeight
function drawStarfish() {


  
}

// Draws arcs to make sea grass
function drawSeagrass() {
  // Setting up the line thickness and color
  stroke(0, 255, 0);
  strokeWeight(10);

  // Draw arcs to make grass


}


// Draw a single fish
function drawFish() {
  // Setting up the line thickness and color
  stroke(250, 125, 0);
  fill(250, 125, 0);
  strokeWeight(30);


  // Fish body


  // Fish tail


}
