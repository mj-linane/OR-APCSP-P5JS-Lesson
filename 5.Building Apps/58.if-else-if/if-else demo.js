/* eslint-disable no-unused-vars */

// IF ELSE AND TYPE CONVERSIONS

// TYPE CONVERSION OPTION #1
let stringNum = Number('3')
stringNum = Number(input.value())

// TYPE CONVERSION OPTION #2
stringNum = '3'
const numNum = Number(stringNum)

// TYPE CONVERSION OPTION #3
if (Number(stringNum) === 3) {
  // some code
}

// IF ELSE CONDITIONAL
if (stringNum === 3) {
  // some code
} else {
  // something else
}
